services:
  - type: worker
    name: telegram-scam-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    plan: free
    envVars:
      - key: BOT_TOKEN
        sync: false  # You set this manually in dashboard
      - key: PYTHON_VERSION
        value: 3.11.0
